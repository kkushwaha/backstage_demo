apiVersion: backstage.io/v1beta2
kind: Template

metadata:
  name: test-template
  title: Test Template
  description: test

spec:
  owner: guests
  type: service

  parameters:
    - title: Example
      required:
        - name
      properties:
        name:
          type: string
          title: Name

  steps:
    - id: log
      name: Log
      action: debug:log
      input:
        message: 'Hello {{ parameters.name }}'
